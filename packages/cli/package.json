{
  "name": "@routeplane/cli",
  "version": "0.1.0",
  "description": "Command-line interface (rp) for the Routeplane AI Gateway — chat, status, and model catalog with profile-based config.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "rp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@routeplane/sdk": "workspace:*",
    "commander": "^12"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.5"
  }
}
