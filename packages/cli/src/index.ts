#!/usr/bin/env node
/**
 * @routeplane/cli — Phase 2 placeholder.
 *
 * The `rp` command-line interface will be implemented in a later phase on top of
 * `@routeplane/sdk`. This entry exists so the workspace wiring, the `bin`
 * mapping, and the build pipeline are in place ahead of that work.
 */

console.log('routeplane CLI (rp) — coming in Phase 2. See https://routeplane.ai');
